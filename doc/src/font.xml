<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE module SYSTEM "api.dtd">

<module name="pygame2.font">
  <short>A module used to find system fonts</short>
  <desc>A module used to find system fonts</desc>

  <example></example>
  <func name="find_font">
    <call>find_fonts (name, bold=False, italic=False, ftype=None) -> [ (str, bool, bool), ... ]</call>
    <desc>
      Finds all fonts matching a certain family or font filename.

      Tries to find all fonts that match the passed requirements best. The
      *name* argument denotes a specific font or font family name. If multiple
      fonts match that name, the *bold* and *italic* arguments are used to find
      the fonts that match the requirements best. *ftype* is an optional font
      filetype argument to request specific font file types, such as bdf or ttf
      fonts.

      All found fonts are returned as list.
    </desc>
    <example></example>
  </func>
  <func name="find_font">
    <call>find_font (name, bold=False, italic=False, ftype=None) -> str, bool, bool</call>
    <desc>
      Finds a font matching a certain family or font filename.

      Tries to find a font that matches the passed requirements best. The
      *name* argument denotes a specific font or font family name. If multiple
      fonts match that name, the *bold* and *italic* arguments are used to find
      a font that matches the requirements best. *ftype* is an optional font
      filetype argument to request specific font file types, such as bdf or ttf
      fonts.
    </desc>
    <example></example>
  </func>
  <func name="get_families">
    <call>get_families () -> [str, str, ...]</call>
    <desc>
      Gets the list of available font families.
    </desc>
    <example></example>
  </func>
</module>

