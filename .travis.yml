# travisci continuous integration config file.

language: python
python:
  - "2.5"
  - "2.6"
  - "2.7"
  - "3.2"
# command to install dependencies
#install:
#  - pip install . --use-mirrors
#  - pip install -r requirements.txt --use-mirrors
# command to run tests
script: 
  - python -m pygame.tests.__main__
before_install:
  - sudo apt-get update
  - sudo apt-get install python-dev libsdl-image1.2-dev libsdl-mixer1.2-dev libsdl-ttf2.0-dev libsdl1.2-dev libsmpeg-dev python-numpy libportmidi-dev ffmpeg libswscale-dev libavformat-dev libavcodec-dev

#notifications:
#  irc:
#    channels: "irc.freenode.org#pygame"
#    on_success: change
#    on_failure: change
